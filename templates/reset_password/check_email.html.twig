{% extends 'base.html.twig' %}

{% block title %}Email envoyé - Linkr{% endblock %}

{% block body %}
<div class="min-h-screen bg-linear-to-br from-gray-50 via-white to-gray-50">
    <main class="flex items-center justify-center min-h-screen py-16 px-4 sm:px-6 lg:px-8">
        <div class="w-full max-w-lg">
            <div class="bg-white rounded-2xl border border-gray-100 shadow-lg shadow-gray-200/50 p-8">
                <h1 class="text-3xl font-extrabold text-gray-900 mb-2 tracking-tight">Email envoyé</h1>
                <p class="text-gray-600 text-sm mb-6">
                    Si un compte correspond à cette adresse email, un message vient d’être envoyé avec un lien pour réinitialiser votre mot de passe.
                    Ce lien expire dans {{ resetToken.expirationMessageKey|trans(resetToken.expirationMessageData, 'ResetPasswordBundle') }}.
                </p>
                <p class="text-gray-600 text-sm mb-8">
                    Si vous ne recevez pas l’email, vérifiez vos spams ou
                    <a href="{{ path('app_forgot_password_request') }}" class="font-semibold text-indigo-600 hover:text-indigo-700 transition-colors">réessayez</a>.
                </p>

                <div class="text-center">
                    <a
                        href="{{ path('app_login') }}"
                        class="inline-block w-full px-8 py-3 bg-linear-to-r from-slate-900 via-indigo-900 to-slate-900 text-white font-semibold rounded-full text-sm tracking-wide shadow-lg shadow-indigo-900/25 hover:shadow-xl hover:shadow-indigo-900/35 transition-all duration-300 hover:scale-105 text-center"
                    >
                        Retour à la connexion
                    </a>
                </div>
            </div>
        </div>
    </main>
</div>
{% endblock %}
